#ifndef QUAD_HPP
#define QUAD_HPP

#include <iostream>
#include <map>
#include <functional>

using namespace std;

class Quad {
    public:
        bool quadCircle();
        bool quadTriangle();
        bool quadRectangle();
};

#endif